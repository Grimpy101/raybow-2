use crate::AppParameters;

pub struct SceneData {}

/// Preparation stage before rendering
///
/// ## Parameters
/// * `parameters` - application parameters
pub fn prepare_render_data(_parameters: &AppParameters) -> SceneData {
    SceneData {}
}
